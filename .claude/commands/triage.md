Triage the challenge at: $ARGUMENTS

Call `kraken_triage` with the path and give me a clear breakdown:

- **Binary**: file type, architecture, protections, packed?
- **Strings**: flag-format matches, crypto keywords, success/fail messages, URLs
- **Files**: list all challenge files with types and sizes
- **Remote**: any detected server endpoints
- **Encoding**: any detected encoded data (base64, hex, custom)
- **Recommendation**: what type of challenge this likely is (constraint, crypto, dynamic, etc.) and which tools to try first
